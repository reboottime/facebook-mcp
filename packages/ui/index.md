# packages/ui — Design System Reference

Read this before using or adding components.

## Components

| Component                   | Exports                                                                                                                                                                                                                                                                                                                                    | Path                                    |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------- |
| **BrandMark**               | `BrandMark`, `BrandMarkSquare`, `BrandMarkProps`, `brandMarkVariants` — canonical notched-viewport SVG mark + gold dot lockup; `wordmark` prop (default `true`); sizes: `default` (mark 20px, wordmark 14px — hero/onboarding), `sm` (mark 16px, wordmark 12px — auth-card); `BrandMarkSquare` = `wordmark={false}` convenience; dot is always `bg-brand` (only chromatic occurrence); `role="img" aria-label="Listo"` | `components/brand-mark.tsx`             |
| **Breadcrumb**              | `Breadcrumb`, `Breadcrumb.Item`, `Breadcrumb.Link`, `Breadcrumb.Page`, `Breadcrumb.Separator`, `BreadcrumbLinkProps` — compound API (no `items={[...]}` array prop), mirrors `SegmentedControl`'s `Object.assign(Root, { Item })` convention; `nav aria-label="Breadcrumb"` wrapping an internal `ol`/`li` (no separate `List` part exposed — two-level restraint by design, no overflow/ellipsis machinery); `Breadcrumb.Link` takes `asChild` (Radix `Slot`) so the package never imports `next/link` — caller passes its own `Link`; `Breadcrumb.Page` renders `aria-current="page"`, not a link, not a tab stop; `Breadcrumb.Separator` is a decorative `aria-hidden` `ChevronRight`; zero domain knowledge — labels/hrefs come from the call site | `components/breadcrumb.tsx`             |
| **Button**                  | `Button`, `buttonVariants`, `ButtonProps` — variants: `primary` (solid), `outline` (border+transparent), `ghost` (text-only), `accent` (solid, secondary brand action), `accent-soft` (tint+border), `destructive`, `destructive-ghost`; `secondary` kept as a deprecated alias of `outline` for existing internal DS consumers (dialog.tsx, alert-dialog.tsx, range-popover-base.tsx) — new call sites use `outline`. Colors come from component-scoped `--color-btn-*` tokens (neutral ink-scale defaults in packages/ui; apps map them, e.g. apps/portal/src/app/globals.css → ink/wa-green). One size only: 32px (no size prop); use `IconButton` for icon-only                                                                                                                                    | `components/button.tsx`                 |
| **Badge**                   | `Badge`, `badgeVariants`, `BadgeProps` — ADS Lozenge anatomy: `text-[11px]`/`leading-3`/`tracking-[.165px]`/`font-bold`, `rounded-badge` (3px), sentence case (not ADS uppercase — standing rule); variants: `neutral`, `success`, `running`, `info`, `beta`, `warning`, `destructive`, `brand-soft`; sizes: `md` (default — `py-px`), `sm` (compact — no vertical padding + `leading-none`, hugs text for dense inline use); `showDot` prop (dot hidden for `info`/`beta`/`neutral`/`brand-soft`); `bare` prop strips chip chrome (bg, border, padding) | `components/badge.tsx`                  |
| **IconButton**              | `IconButton`, `iconButtonVariants`, `IconButtonProps` — square icon-only button; `aria-label` required at type level; variants: `primary`, `secondary`, `ghost`, `destructive`, `destructive-ghost`; sizes: `md` (32×32, default — toolbar/card), `sm` (24×24 — table row trailing actions only)                                        | `components/icon-button.tsx`            |
| **AlertDialog**             | `AlertDialog`, `AlertDialogTrigger`, `AlertDialogPortal`, `AlertDialogOverlay`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`, `AlertDialogActionProps` — Radix `AlertDialog` (`role="alertdialog"`, stricter focus trap than `Dialog`); composable subcomponent API; `AlertDialogAction` accepts `variant` prop defaulting to `"destructive"` (the common case); `AlertDialogCancel` is always `secondary`; no size prop, no close button (requires user response); overlay/content motion matches `Dialog`; max-w 480px centered | `components/alert-dialog.tsx`           |
| **Dialog**                  | `Dialog`, `DialogTrigger`, `DialogClose`, `DialogPortal`, `DialogOverlay`, `DialogContent`, `DialogHeader`, `DialogBody`, `DialogFooter`, `DialogTitle`, `DialogDescription` — no size prop; callers set `max-w-*` via className                                                                                                           | `components/dialog/`                    |
| **Drawer**                  | `Drawer`, `DrawerTrigger`, `DrawerClose`, `DrawerPortal`, `DrawerOverlay`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`, `DrawerDescription`, `DrawerBody`, `DrawerFooter`, `drawerPanelVariants`, `DrawerPanelVariantProps`, `DrawerProps`, `DrawerContentProps`, `DrawerSide` — sides: `right` (default), `left`, `bottom`; sizes: `sm` (320px), `md` (480px, default), `lg` (640px), `full`; `modal` prop (default `true`); `draggable` prop for bottom drag-to-dismiss (default `false`); non-modal is right/left only | `components/drawer/`                    |
| **Icon**                    | `Icon`, `IconProps` — thin wrapper around any `lucide-react` icon component; props: `icon: LucideIcon`, `size?: "sm" \| "md"` (default `"md"`), `label?: string` (undefined → decorative with `aria-hidden="true"`; defined → `role="img" aria-label={label}`), `strokeWidth?: number` (default `2`), `className?: string`; always `currentColor` | `components/icon/` |
| **Input**                   | `Input`, `InputProps` — one size: 32px (no size prop); supports `leading`/`trailing` slot props, `disabled`/`readOnly`/`aria-invalid` states                                                                                                                                                                                               | `components/input.tsx`                  |
| **SearchInput**             | `SearchInput`, `SearchInputProps` — thin wrapper over `Input` with `type="search"` baked in and a default `Search` (lucide) leading icon; `icon` slot prop overrides the icon; `aria-label` or `aria-labelledby` required at the type level (discriminated union); all other `InputProps` (size, value, onChange, disabled, aria-invalid, className) pass through | `components/search-input.tsx`           |
| **Command**                 | `Command`, `CommandDialog`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`, `CommandSeparator`, `CommandShortcut` — `cmdk`-powered fuzzy search + keyboard nav; re-skinned to design tokens; no standalone usage — consumed by `Combobox` and `MultiSelect` (or custom command palettes) | `components/command.tsx`                |
| **Combobox**                | `Combobox`, `ComboboxOption`, `ComboboxGroup`, `ComboboxProps`, `ComboboxTwoLineOption` — single-value searchable dropdown; trigger IS the search input (no separate search field); composed from `Popover` + `Command`; trigger visually matches `SelectTrigger`; controlled (`value` + `onValueChange`); supports flat `options` or grouped `groups` (mutually exclusive); backspace-to-empty calls `onValueChange(null)`; sizes: `sm`, `md`; match-sorter CONTAINS filtering; optional `renderOption` prop for custom row content (two-line: use `ComboboxTwoLineOption`); `label?: string` prop renders a muted dimension prefix ("Role: Owner") inside the trigger; hover/focus-revealed clear (×) button appears when a value is selected — cross-fades with chevron, `aria-label="Clear {label ?? 'selection'}"`, keyboard-reachable | `components/combobox.tsx`               |
| **MultiSelect**             | `MultiSelect`, `MultiSelectTrigger`, `MultiSelectOption`, `MultiSelectProps` — multi-value searchable dropdown; `Checkbox` per row; select-all / clear header (scoped to filtered set); chip overflow (`maxChips` default 2, shows `+N`); popover stays open on item click; sizes: `sm`, `default` | `components/multi-select.tsx`           |
| **Select**                  | `Select`, `SelectGroup`, `SelectValue`, `SelectTrigger`, `SelectScrollUpButton`, `SelectScrollDownButton`, `SelectContent`, `SelectLabel`, `SelectItem`, `SelectSeparator` — Radix Select, no built-in search filter                                                                                                                      | `components/select/`                    |
| **Slider**                  | `Slider`, `SliderProps` — single-value Radix Slider; props: `value: number`, `onValueChange: (v: number) => void`, `min` (default 0), `max` (default 100), `step` (default 1), `disabled`, `aria-label`, `aria-labelledby`, `className`; thumb: 14×14 rounded-full bg-background border-2 border-primary; track: 4px bg-secondary; range fill: bg-primary; focus ring via global base.css | `components/slider.tsx`                 |
| **Collapsible**             | `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`, `CollapsibleProps`, `CollapsibleTriggerProps`, `CollapsibleContentProps` — Radix Collapsible wrapper; trigger is UNSTYLED (callers compose Button + chevron via `asChild`); content animates open/close via CSS grid-template-rows (0fr↔1fr), no JS measuring; enter: duration-base + ease-out-emphasized; exit: duration-fast + ease-in-accelerated | `components/collapsible.tsx`            |
| **Switch**                  | `Switch` — Radix Switch; checked/unchecked track colors via `--primary`/`--muted`; sizes: `md` (40×22px, default), `sm` (32×18px)                                                                                                                                                             | `components/switch.tsx`                 |
| **Tabs**                    | `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` — Radix Tabs (tablist/tab/tabpanel ARIA); `TabsList` `variant: "segmented" \| "underline"` (default `segmented`) context-shared to `TabsTrigger`; both variants use a JS-measured sliding indicator (`useSlideIndicator`, ResizeObserver + font-ready remeasure) rather than per-trigger active styling; `underline` matches ADS Tabs anatomy (border-block-end 2px, ink state tokens — see file header citation); `segmented` has no ADS source and duplicates `SegmentedControl`'s sliding-thumb approach — zero consumers in `apps/portal/src` (verified 2026-07-23); use `SegmentedControl` for a real segmented-look control today | `components/tabs.tsx`                   |
| **SegmentedControl**        | `SegmentedControl`, `SegmentedControl.Item`, `segmentedControlRootVariants`, `segmentedControlItemVariants`, `SegmentedControlProps`, `SegmentedControlOption` — Radix ToggleGroup `type="single"` + deselect guard; compound API `<SegmentedControl value onValueChange aria-label><SegmentedControl.Item value>`; no `size` prop — fixed `h-8`; renders as a connected bordered button group (Jira toolbar look) — `rounded-sm` outer border, `border-r` divider between items, no sliding thumb/track; selected item `bg-selected-surface`/`text-selected-foreground`; disabled item via Radix `disabled`, active item cannot be individually disabled; keyboard: arrow keys (Radix native), Space/Enter activate; no ADS package ships this shape (`@atlaskit/segmented-control` 404s on unpkg) | `components/segmented-control.tsx`      |
| **Toaster (toast)**         | `Toaster` (render once at root), re-exports `toast` imperative API from `sonner` — variants: `success`, `info`, `warning`, `error`; file: `toast.tsx` (renamed from `sonner.tsx`)                                                                                                                                                         | `components/toast.tsx`                  |
| **Checkbox**                | `Checkbox`, `checkboxVariants`, `CheckboxProps` — Radix Checkbox; sizes: `md` (20px, default — form rows), `sm` (18px — table row bulk-select), `xs` (14px — compact column header "select all"); indeterminate state via `checked="indeterminate"` | `components/checkbox.tsx` |
| **Table**                   | React components: `Table` (root, `density: default\|compact`, `totalCount` + `pageOffset` required), `TableHeader`, `TableHeaderCell` (`sortable`, `label`, `sticky: left\|right\|none`, `numeric`), `TableBody`, `TableRow` (`selected`, `pinned`, `outcome`, `onDrill`), `TableCell` (`sticky`, `variant: default\|id\|numeric\|status`), `TableSelectionBar` (`count`, `actions`, `onClear`), `TableEmptyRow` (`cliCommand`, `docHref`), `TableErrorBand` (`cause`, `onRetry`), `TableSkeletonRow` (`colCount`). CSS-only legacy exports also present: `tableClass`, `tableHeadVariants`, `tableRowVariants`, `tableCellVariants`, etc. | `components/table.tsx` |
| **Card**                    | `Card`, `cardVariants`, `CardVariantProps`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardProps`, `CardHeaderProps`, `CardTitleProps`, `CardDescriptionProps`, `CardContentProps`, `CardFooterProps` — flat by default (no shadow); `elevated` boolean adds `--shadow-card`; `interactive` boolean adds hover/focus/active states; `dense` boolean sets `data-density="dense"` for compact 12px padding via CSS attribute selectors on child slots; `CardTitle` accepts `as` prop for semantic heading level | `components/card/`                      |
| **Popover**                 | `Popover`, `PopoverTrigger`, `PopoverAnchor`, `PopoverContent`, `PopoverContentProps` — Radix Popover; `--popover` surface + `--shadow-popover` + `--radius-lg`; no arrow caret; no header/footer/close slots (body fully caller-controlled); `sideOffset` defaults to `8`; animation: `--duration-base` (180ms) + `--ease-out`, slide-from-anchor + fade; focus trapped while open, returns to trigger on Escape; for `aria-labelledby` wire to caller's heading id | `components/popover/`                   |
| **Separator**               | `Separator`                                                                                                                                                                                                                                                                                                                                | `components/separator/`                 |
| **StatusBadge**             | `StatusBadge`, `statusBadgeVariants`, `StatusBadgeProps` — Option C design: 8px colored dot + `--color-text-primary` label, no pill; dot colors: confirmed=`--color-tx-confirmed`, pending=`--color-tx-pending`, failed=`--color-tx-failed`, unknown=`--color-tx-unknown`; unknown label uses `--color-text-secondary`                     | `components/status-badge/`              |
| **HashDisplay**             | `HashDisplay`, `hashDisplayVariants`, `HashDisplayProps` — `prefixChars` defaults to 6, `suffixChars` to 4 (middle-out `0x6f52a1…c7b3`); `copyOnHover` prop hides copy icon at rest (requires parent `group`); `label` prop renders human label instead of truncated hash                                                                  | `components/hash-display/`              |
| **AddressChip**             | `AddressChip`, `AddressChipProps` — delegates to `HashDisplay`; supports `copyOnHover` prop (pass to parent `group` rows for hover-reveal copy icon, matching HashDisplay TXN HASH column behavior); `label` + `iconSrc` for entity display (TitanBuilder, Uniswap V2, etc.)                                                               | `components/address-chip/`              |
| **TokenChip**               | `TokenChip`, `tokenChipVariants`, `TokenChipProps`                                                                                                                                                                                                                                                                                         | `components/token-chip/`                |
| **MethodBadge**             | `MethodBadge`, `methodBadgeVariants`, `MethodBadgeProps`, `MethodCategory` — no pill shape; transfer = `--color-text-tertiary` plain text; default (mint/approve/swap/burn) = `--color-text-secondary` weight-500 text (indigo reserved for interactive affordances only)                                                                  | `components/method-badge/`              |
| **DirectionBadge**          | `DirectionBadge`, `directionBadgeVariants`, `DirectionBadgeProps`                                                                                                                                                                                                                                                                          | `components/direction-badge/`           |
| **RankBadge**               | `RankBadge`, `RankBadgeProps`                                                                                                                                                                                                                                                                                                              | `components/rank-badge/`                |
| **LabelBadge**              | `LabelBadge`, `LabelBadgeProps`                                                                                                                                                                                                                                                                                                            | `components/label-badge/`               |
| **PriceChange**             | `PriceChange`, `priceChangeVariants`, `PriceChangeProps`, `PriceDirection`                                                                                                                                                                                                                                                                 | `components/price-change/`              |
| **StatsCard**               | `StatsCard`, `StatsCardProps` — `secondaryValue` prop for ETH+USD pair display; `change` slot is optional and leaves no empty space when absent                                                                                                                                                                                            | `components/stats-card/`                |
| **MiniChart**               | `MiniChart`, `MiniChartProps`                                                                                                                                                                                                                                                                                                              | `components/mini-chart/`                |
| **DataTable**               | `DataTable`, `dataTableVariants`, `Th`, `Td`, `Tr`, `DataTableProps`, `ThProps`, `TdProps`, `TrProps` — `Th`: 11px headers, widened tracking, weight-500, tertiary; `Td`: `align` prop + `numeric` for tabular-nums; `Tr`: tbody row with built-in `hover:bg-[--color-surface-row-hover]`                                                  | `components/data-table/`                |
| **Pagination**              | `Pagination`, `pageItemVariants`, `PaginationProps`, `RankPagination`, `RankPaginationProps` — active page uses `--color-surface-row-hover` fill with `--color-text-primary` text (neutral current-location indicator, not indigo); full variant includes First/Last buttons                                                               | `components/pagination/`                |
| **TabBar**                  | `TabBar`, `TabBarProps`, `TabItem`                                                                                                                                                                                                                                                                                                         | `components/tab-bar/`                   |
| **KeyValueTable**           | `KeyValueTable`, `KeyValueTableProps`, `KeyValueRow`                                                                                                                                                                                                                                                                                       | `components/key-value-table/`           |
| **StatusBlock**             | `StatusBlock`, `statusBlockVariants`, `StatusBlockProps` — tinted card-like container; `tone: "default" \| "destructive"`; `default` = neutral `bg-card` + hairline `border-border` top-border; `destructive` = `bg-destructive/5` + `border-destructive/30` top-border + inheritable `text-destructive`; free `children` composition; no sub-components | `components/status-block.tsx`           |
| **EmptyState**              | `EmptyState`, `EmptyStateProps`, `EmptyStateVariant`, `EmptyStateSize` — centered feedback block for empty collections/filter results; variants: `zero-state` (icon container, collection genuinely empty), `no-results` (no icon, filter returned nothing); sizes: `md` (default, py-12, 48×48 icon container), `sm` (py-6, icon suppressed — for combobox/command); props: `variant`, `icon` (Lucide), `title`, `subtitle?`, `cta?` (ReactNode), `size?`; `role="status"` | `components/empty-state.tsx` |
| **ErrorState**              | `ErrorState`, `ErrorStateProps` — centered feedback block for content-region load failures; single size (full panels only); default icon `AlertCircle` (Lucide), suppressible via `icon={null}`; icon container: 48×48 rounded-full, `bg-state-errored-subtle`; props: `title`, `subtitle?`, `icon?` (Lucide or null), `action?` (ReactNode); `role="alert" aria-live="assertive"` | `components/error-state.tsx` |
| **Alert**                   | `Alert`, `AlertTitle`, `AlertDescription` — inline alert banner; variants: `default` (neutral muted), `destructive` (red/errored), `warning` (amber), `success` (green), `info` (blue); variant drives background, border, icon color, and title color; description always muted-foreground; icon slot via `has-[>svg]` grid selector; variant context via React context so `AlertTitle` reads it without an extra prop | `components/alert.tsx`                  |
| **AlertBanner**             | `AlertBanner`, `alertBannerVariants`, `AlertBannerProps`                                                                                                                                                                                                                                                                                   | `components/alert-banner/`              |
| **DownloadButton**          | `DownloadButton`, `DownloadButtonProps`                                                                                                                                                                                                                                                                                                    | `components/download-button/`           |
| **TxCard**                  | `TxCard`, `TxCardList`, `TxCardProps`, `TxCardListProps`                                                                                                                                                                                                                                                                                   | `components/tx-card/`                   |
| **AccountCard**             | `AccountCard`, `AccountCardList`, `AccountCardProps`, `AccountCardListProps`                                                                                                                                                                                                                                                               | `components/account-card/`              |
| **ActionBanner**            | `ActionBanner`, `ActionBannerProps`                                                                                                                                                                                                                                                                                                        | `components/action-banner/`             |
| **TickerBar**               | `TickerBar`, `TickerBarProps`                                                                                                                                                                                                                                                                                                              | `components/ticker-bar/`                |
| **GlobalHeader**            | `GlobalHeader`, `GlobalHeaderProps`, `NavItem`                                                                                                                                                                                                                                                                                             | `components/global-header/`             |
| **Footer**                  | `Footer`, `FooterProps`, `FooterColumn`                                                                                                                                                                                                                                                                                                    | `components/footer/`                    |
| **BackToTop**               | `BackToTop`, `BackToTopProps`                                                                                                                                                                                                                                                                                                              | `components/back-to-top/`               |
| **NetworkSwitcherDropdown** | `NetworkSwitcherDropdown`, `NetworkSwitcherDropdownProps`, `NetworkOption`                                                                                                                                                                                                                                                                 | `components/network-switcher-dropdown/` |
| **ExpandCollapse**          | `ExpandCollapse`, `ExpandCollapseProps`                                                                                                                                                                                                                                                                                                    | `components/expand-collapse/`           |
| **BlockNavArrows**          | `BlockNavArrows`, `BlockNavArrowsProps`                                                                                                                                                                                                                                                                                                    | `components/block-nav-arrows/`          |
| **NFTMediaFrame**           | `NFTMediaFrame`, `NFTMediaFrameProps`, `NFTMediaType`                                                                                                                                                                                                                                                                                      | `components/nft-media-frame/`           |
| **SearchBar**               | `SearchBar`, `SearchBarProps`, `SearchFilterPrefix`, `SearchResult` — supports `variant` prop (`"default"` \| `"compact"`); compact: 36px height, Enter-to-submit, no submit button, inline mode                                                                                                                                           | `components/search-bar/`                |
| **TokenHoldingsExpander**   | `TokenHoldingsExpander`, `TokenHoldingsExpanderProps`, `TokenHolding`                                                                                                                                                                                                                                                                      | `components/token-holdings-expander/`   |
| **AdvancedFilterDrawer**    | `AdvancedFilterDrawer`, `AdvancedFilterDrawerProps`, `FilterGroup`, `FilterOption`                                                                                                                                                                                                                                                         | `components/advanced-filter-drawer/`    |
| **PriceChart**              | `PriceChart`, `PriceChartProps`, `TimeRange`, `PricePoint`                                                                                                                                                                                                                                                                                 | `components/price-chart/`               |
| **BlockDetailHeader**       | `BlockDetailHeader`, `BlockDetailHeaderProps`, `BlockStat`                                                                                                                                                                                                                                                                                 | `components/block-detail-header/`       |
| **TxDetailHeader**          | `TxDetailHeader`, `TxDetailHeaderProps`                                                                                                                                                                                                                                                                                                    | `components/tx-detail-header/`          |
| **AddressDetailHeader**     | `AddressDetailHeader`, `AddressDetailHeaderProps`                                                                                                                                                                                                                                                                                          | `components/address-detail-header/`     |
| **MultichainStrip**         | `MultichainStrip`, `MultichainStripProps`, `ChainPresence`                                                                                                                                                                                                                                                                                 | `components/multichain-strip/`          |
| **TokenListTable**          | `TokenListTable`, `TokenListTableProps`, `TokenRow`, `TokenSortKey`, `SortDirection`                                                                                                                                                                                                                                                       | `components/token-list-table/`          |
| **TokenDetailHeader**       | `TokenDetailHeader`, `TokenDetailHeaderProps`, `TokenMarketStat`                                                                                                                                                                                                                                                                           | `components/token-detail-header/`       |
| **TokenHoldersList**        | `TokenHoldersList`, `TokenHoldersListProps`, `TokenHolderRow`                                                                                                                                                                                                                                                                              | `components/token-holders-list/`        |
| **TokenTransfersList**      | `TokenTransfersList`, `TokenTransfersListProps`, `TokenTransferRow`                                                                                                                                                                                                                                                                        | `components/token-transfers-list/`      |
| **LeaderboardTable**        | `LeaderboardTable`, `LeaderboardTableProps`, `LeaderboardRow`                                                                                                                                                                                                                                                                              | `components/leaderboard-table/`         |
| **NFTCollectionGrid**       | `NFTCollectionGrid`, `NFTCollectionGridProps`, `NFTCollectionCard`                                                                                                                                                                                                                                                                         | `components/nft-collection-grid/`       |
| **NFTCollectionHeader**     | `NFTCollectionHeader`, `NFTCollectionHeaderProps`, `NFTCollectionStat`                                                                                                                                                                                                                                                                     | `components/nft-collection-header/`     |
| **NFTItemGrid**             | `NFTItemGrid`, `NFTItemGridProps`, `NFTItem`                                                                                                                                                                                                                                                                                               | `components/nft-item-grid/`             |
| **NFTItemHeader**           | `NFTItemHeader`, `NFTItemHeaderProps`, `NFTAttribute`                                                                                                                                                                                                                                                                                      | `components/nft-item-header/`           |
| **NFTTransferHistory**      | `NFTTransferHistory`, `NFTTransferHistoryProps`, `NFTTransferRow`                                                                                                                                                                                                                                                                          | `components/nft-transfer-history/`      |
| **StatRow**                 | `StatRow`, `StatRowProps`                                                                                                                                                                                                                                                                                                                  | `components/stat-row/`                  |
| **SplitRow**                | `SplitRow`, `SplitRowProps`                                                                                                                                                                                                                                                                                                                | `components/split-row/`                 |
| **SectionHeader**           | `SectionHeader`, `SectionHeaderProps`                                                                                                                                                                                                                                                                                                      | `components/section-header/`            |
| **LiveFeed**                | `LiveFeed`, `LiveFeedRow`, `LiveFeedProps`, `LiveFeedRowProps`                                                                                                                                                                                                                                                                             | `components/live-feed/`                 |
| **Tooltip**                 | `Tooltip`, `TooltipProvider`, `TooltipRoot`, `TooltipTrigger`, `TooltipContent`, `TooltipProps` — Radix-based; convenience `<Tooltip content="…">` wrapper + primitive exports for custom composition                                                                                                                                      | `components/tooltip/`                   |
| **Spinner**                 | `Spinner`, `spinnerVariants`, `SpinnerProps`, `SpinnerVariantProps` — SVG arc spinner; sizes: `sm` (14px), `md` (18px, default), `lg` (24px); color inherits `currentColor`; `aria-label` prop (default "Loading"); pass `aria-hidden="true"` when nested inside Button loading state                                      | `components/spinner/`                   |
| **Progress**                | `Progress`, `progressTrackVariants`, `progressFillVariants`, `ProgressProps` — Radix Progress; `state: "default" \| "success" \| "warning" \| "error" \| "neutral"` (default `neutral`) controls fill color only, tree is identical; `size: "sm"` (4px, default) \| `"md"` (6px); `indeterminate` prop (or omit `value`) renders a 40%-wide animated sweep; optional `label`/`valueLabel` row above the track; `state="default"` renders a teal gradient + glow (deliberate Listo choice, not ADS-derived); zero consumers in `apps/portal/src` (verified 2026-07-23); `aria-label`/`aria-labelledby` required by caller | `components/progress.tsx`               |
| **StarCount**               | `StarCount`, `StarCountProps`, `starToggleVariants`, `starCountWrapperVariants` — toggleable star button + adjacent numeric count; controlled via `pressed` + `onPressedChange`; `label` prop drives `aria-label` ("Star {label}" / "Unstar {label}"); sizes: `sm` (h-7, default) and `md` (h-8); active star: `fill-brand text-brand`; inactive: `text-meta-foreground`; `stopPropagation` defaults to `true` (prevents card/row link activation) | `components/star-count.tsx`             |
| **CopyButton**              | `CopyButton`, `CopyButtonProps` — ghost xs IconButton + Tooltip; idle Copy icon → copied Check icon (success color) → revert after 1500ms; `value` (clipboard string), `ariaLabel?`, `tooltipLabel?` (default `"Copy"`), `tooltipCopiedLabel?` (default `"Copied!"`), `disabled?`, `className?`; no default idle color override (callers pass className to tint); see `lib/use-copy-to-clipboard` for the standalone hook | `components/copy-button.tsx`            |
| **VisibilityIcon**          | `VisibilityIcon`, `VisibilityIconProps`, `visibilityIconVariants` — read-only labeled graphic (`role="img"`) showing `Globe` (public) or `Lock` (private) from lucide-react; `visibility: "public" \| "private"` required; tooltip always rendered, text = `"Visibility: Public"` / `"Visibility: Private"` (matches `aria-label`); sizes: `sm` (14px icon, default), `md` (16px icon); `className` forwarded to trigger span; requires ancestor `<TooltipProvider>` | `components/visibility-icon.tsx`        |
| **Chip + ChipGroup**        | `Chip`, `ChipGroup`, `ChipProps`, `ChipGroupProps`, `chipVariants` — generic multi-select toggle pill; `rounded-sm` (4px, matches ADS Tag radius, not Tailwind's default 6px); `<Chip checked onCheckedChange>children</Chip>` standalone, or `<ChipGroup value onChange><Chip value>children</Chip></ChipGroup>` for managed multi-select; children slot only (no icon prop — consumers compose `<Icon /> label`); selected: `bg-primary-soft border-primary-border`; unselected: `bg-background border-border` + hover lifts to `border-border-strong`; `aria-pressed` on each chip; `role="group"` on container; arrow-key roving focus in group; multi-select only (single mode not yet implemented) | `components/chip.tsx` |
| **Calendar**                | `Calendar`, `CalendarProps`, `DateRange` — thin styled wrapper over react-day-picker v10 `DayPicker`; supports `mode: single \| multiple \| range`; no default CSS import from react-day-picker; tokens: `bg-selected-surface` for range start/end, `bg-primary-soft` for range middle, `text-primary` for today; nav arrows use `ChevronLeftIcon`/`ChevronRightIcon` (lucide); re-exported `DateRange` type from react-day-picker; focus managed by react-day-picker's `DayFlag.focused` → `shadow-focus-ring` via classNames | `components/calendar.tsx`               |
| **DateRangeSelector**       | `DateRangeSelector`, `DateRangeSelectorProps`, `DateTimeRange`, `RangePreset`, `DateRangeSelectorPreset` — date-only range picker popover; two-column layout: preset list (consumer-supplied `presets[]`) + calendar grid (range mode); state machine: `idle → drawing (first day click) → complete (second click) → committed (Apply) \| preset-committed (preset click)`; presets commit immediately via `onChange` + close; custom range requires Apply; Apply disabled while drawing or `end < start`; inline error `"End must be after start"` in `text-state-errored-text`; roving DOM focus on preset listbox (no `aria-activedescendant`); no hardcoded presets | `components/date-range-selector.tsx`    |
| **DateTimeRangeSelector**   | `DateTimeRangeSelector`, `DateTimeRangeSelectorProps`, `DateTimeRangeSelectorPreset`, `DateTimeRangeSelectorTimezoneOption` — date+time+timezone range picker popover; three-column layout: preset list + calendar grid + time column; time column: IANA timezone `Select` (or plain text when `timezoneOptions` omitted) + FROM/TO date+time `Input` pairs + optional "Remove time" `Button`; same state machine as `DateRangeSelector`; `onRemoveTime` fires when user wants to collapse to date-only (consumer switches component); same a11y model | `components/date-time-range-selector.tsx` |

## Design Tokens

Visual design documentation for designers and developers. View in Storybook under "Design Tokens"
section.

| Category         | Description                                            | Path                                      |
| ---------------- | ------------------------------------------------------ | ----------------------------------------- |
| **Introduction** | Overview and usage guidelines                          | `stories/tokens/Introduction.stories.tsx` |
| **Colors**       | Surface, text, border, status, action, and icon colors | `stories/tokens/Colors.stories.tsx`       |
| **Typography**   | Font families, type scale, weights, letter spacing     | `stories/tokens/Typography.stories.tsx`   |
| **Spacing**      | 8px grid scale, container widths, breakpoints          | `stories/tokens/Spacing.stories.tsx`      |
| **Elevation**    | Shadow tokens and z-index hierarchy                    | `stories/tokens/Elevation.stories.tsx`    |

**Source files:** `packages/ui/src/styles/tokens/`

## Utilities

| Export                    | Path     |
| ------------------------- | -------- |
| `cn` (class merge helper) | `lib/cn` |
| `useCopyToClipboard`      | `lib/use-copy-to-clipboard` |
| `overlayPanelSurface`, `overlayPanelHeaderBase`, `overlayPanelHeaderScrolled`, `overlayPanelBody`, `overlayPanelFooterBase`, `overlayPanelFooterScrolled`, `overlayPanelTitle`, `overlayPanelDescription` — shared class-string constants for overlay panel anatomy (Dialog + Drawer). Compose via `cn()`. | `lib/overlay-panel` |

## Usage

```tsx
import { Button, Card, CardContent, Input, cn } from "@repo/ui";
```

## Adding Components

Use the `/add-component` skill. Each component lives in `src/components/<name>/` with:

- `index.tsx` — component implementation
- `<name>.stories.tsx` — Storybook stories
- `<name>.test.tsx` — tests

After adding, export from `src/index.ts` and update this file.

## Conventions

### No border on elevated surfaces

Elevated surfaces — `Dialog`, `Drawer`, `Popover`, `DropdownMenu` Content + SubContent, `Select` Content, `Command` + `CommandDialog`, `Combobox` listbox, `Tooltip`, `Toast` — define their perimeter via the elevation shadow token only. Do not add `border border-border` (or any variant) to the panel element itself.

**Why.** Each of `--shadow-1`, `--shadow-2`, `--shadow-3` starts with a `0 0 0 1px` ring layer. That ring is the canonical hairline. Stacking a CSS border on the same element places a darker (~11 % alpha) 1 px line immediately against the lighter (~6 % alpha) shadow ring — perceived double edge.

**Still fine.** Internal row/item separators, directional seam borders (`border-r` on a right-side drawer), sticky-scroll cue borders, focus rings, inline form-field borders. The violation is only a full-perimeter border on the elevated panel itself.

**Precedent.** `dialog.tsx` documents this rule inline (search for `"double edge"`). The same comment string is mirrored on every elevated-surface primitive that needs it. This section is the canonical source; the inline comment is the proximate guardrail.

## TODO

- [ ] **Visual regression testing** — Unit tests catch behavior but not layout regressions (e.g.,
      icon positioning, spacing changes). Set up Loki for local Storybook visual testing. See:
      https://loki.js.org/
- [ ] **Primitive rename — `--ledger-*` / `--orange-*` names are now historical.** The 2026-07-23
      Atlassian Design System retheme (`@atlaskit/tokens@16.3.0`) repoints these primitives to ADS
      Blue/Neutral values in `packages/ui/src/styles/primitive.css`; the identifiers still describe
      the retired "Kitchen-Table Ledger" warm-paper palette. Deliberately deferred, not bundled with
      the value swap (see `.intermediate/specs/theme/jira-palette-implementation.spec.md` §3b/§5):
      zero `apps/portal` coupling to these names (`var(--ledger-*)` / `var(--orange-*)` return zero
      hits — portal hardcodes its own literals) means the rename buys nothing functionally and would
      only destroy the value-only diff's revert/review granularity. Follow-up: mechanical rename of
      `--ledger-cream`, `--ledger-paper`, `--ledger-ink-900/700/500`, `--ledger-line`,
      `--ledger-line-soft`, `--ledger-hover-tint`, `--ledger-active-tint`, `--ledger-green-800/900/tint`,
      `--ledger-blue`, and `--orange-900/800/700/500/400` to ADS-honest names, touching every
      `theme.css` reference site (~30) in one PR for clean review.
